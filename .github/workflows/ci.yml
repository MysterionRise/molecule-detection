name: CI

on:
  push:
    branches: [main, master, develop, claude/**]
  pull_request:
    branches: [main, master, develop]
  workflow_dispatch:

jobs:
  backend:
    name: Backend Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11']

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd chemvision/backend
          python -m pip install --upgrade pip || true
          pip install -e ".[dev]"

      - name: Run ruff (lint)
        run: |
          cd chemvision/backend
          ruff check .

      - name: Run ruff (format check)
        run: |
          cd chemvision/backend
          ruff format --check .

      - name: Run mypy (type check)
        run: |
          cd chemvision/backend
          mypy app

      - name: Run pytest
        run: |
          cd chemvision/backend
          python -m pytest -v --cov=app --cov-report=term-missing --cov-report=xml

      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          file: ./chemvision/backend/coverage.xml
          flags: backend
          name: backend-coverage

  frontend:
    name: Frontend Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: ['20']

    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
          cache-dependency-path: chemvision/frontend/pnpm-lock.yaml

      - name: Install dependencies
        run: |
          cd chemvision/frontend
          pnpm install --frozen-lockfile

      - name: Run ESLint
        run: |
          cd chemvision/frontend
          pnpm lint

      - name: Run tests
        run: |
          cd chemvision/frontend
          pnpm test --run

      - name: Build
        run: |
          cd chemvision/frontend
          pnpm build

  docker:
    name: Docker Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build backend image
        uses: docker/build-push-action@v5
        with:
          context: ./chemvision/backend
          push: false
          tags: chemvision-backend:test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./chemvision/frontend
          push: false
          tags: chemvision-frontend:test
          cache-from: type=gha
          cache-to: type=gha,mode=max
